1. find the minimum of given 2 numbers
#include <stdio.h>

void main()
{
    int a,b;
    printf("Enter A : ");
    scanf("%d",&a);
    printf("Enter B : ");
    scanf("%d",&b);

    if (a < b)
    {
        printf("%d", a);
    }
    else if (a =b)
    {
        printf("A & B both are same.");
    }
    else
    {
        printf("%d", b);
    }
    
}


2. find out if given number is positive, negative or neutral
#include <stdio.h>
#include <conio.h>

void main()
{
    int a;
    printf("Enter A : ");
    scanf("%d",&a);

    if (a < 0)
    {
        printf("A is Negative.");
    }
    else if (a == 0)
    {
        printf("A is Neutral.");
    }
    else
    {
        printf("A is Positive.");
    }
    
}