#include <stdio.h>

void main() {
    int i, j;
    int start, spaces;

    for(i = 0; i < 5; i++)
    {
        for(spaces = 0; spaces < i; spaces++)
        {
            printf("  ");
        }

        for(j = 10 - i; j >= 6; j--)
        {
            printf("%d ", j);
        }

        printf("\n");
    }
}
